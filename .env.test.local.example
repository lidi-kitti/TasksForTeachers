# Скопируй: cp .env.test.local.example .env.test.local
# Используй те же credentials, что в .env.local (база tasks_tz → tasks_tz_test автоматически)
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@127.0.0.1:5432/tasks_tz?serverVersion=16&charset=utf8"
