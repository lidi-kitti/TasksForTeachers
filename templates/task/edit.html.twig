{% extends 'base.html.twig' %}

{% block title %}Редактирование: {{ task.title }}{% endblock %}

{% block body %}
<h1 class="h3 mb-4">Редактирование задания</h1>

<div class="card shadow-sm">
    <div class="card-body">
        {{ include('task/_form.html.twig', {button_label: 'Сохранить'}) }}
    </div>
</div>
{% endblock %}
