{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="d-flex gap-2">
        <button class="btn btn-primary" type="submit">{{ button_label|default('Сохранить') }}</button>
        <a href="{{ path('app_task_index') }}" class="btn btn-outline-secondary">Отмена</a>
    </div>
{{ form_end(form) }}
